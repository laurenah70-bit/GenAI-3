# 프로젝트 1: 자동화 도구 비교 구현
### 고객 문의 접수 자동 분류 및 알림 워크플로우

---

## 1. 개요

- **워크플로우 목적**: 외부에서 접수되는 고객 문의를 긴급도(urgency)에 따라 자동 분류하여 스프레드시트에 기록하고, 담당 채널에 Slack 알림을 발송한다.
- **비교 대상 도구**: Make, Zapier
- **구현 조합**: Webhook(Trigger) + Router/Filter(조건 분기) + Google Sheets(기록) + Slack(알림)

---

## 2. 워크플로우 설계

```
[Trigger] Webhook으로 문의 데이터 수신
     │
     ▼
[조건 분기] urgency 필드 확인
     │                    │
   "high"                "low"
     │                    │
     ▼                    ▼
[Action 1a] Sheets      [Action 1b] Sheets
"긴급문의" 탭에 기록      "일반문의" 탭에 기록
     │                    │
     ▼                    ▼
[Action 2a] Slack        [Action 2b] Slack
🚨긴급 채널 알림           일반 채널 알림
```

**테스트용 Webhook 페이로드 (JSON)**
```json
{
  "name": "홍길동",
  "email": "hong***@example.com",
  "category": "환불문의",
  "urgency": "high",
  "message": "결제 후 상품이 오지 않았습니다."
}
```
`urgency` 값을 `high` / `low`로 바꿔 각각 1회 이상 전송하여 두 분기가 모두 실행되었음을 확인한다.

---

## 3. [Make] 구현

| 단계 | 모듈 | 설정 요약 |
|---|---|---|
| Trigger | Webhooks – Custom webhook | Webhook URL 발급 후 테스트 페이로드 전송 |
| 조건 분기 | Router | 2개 경로로 분기, 각 경로에 Filter(`urgency = high`, `urgency = low`) 설정 |
| Action 1a | Google Sheets – Add a Row | "긴급문의" 탭에 name/email/category/message 기록 |
| Action 2a | Slack – Create a Message | 🚨긴급 채널에 알림 |
| Action 1b | Google Sheets – Add a Row | "일반문의" 탭에 기록 |
| Action 2b | Slack – Create a Message | 일반 채널에 알림 |

### 워크플로우 구성 화면
> 📸 *[여기에 Make 시나리오 편집 화면 스크린샷 삽입 — Webhook, Router, Sheets, Slack 모듈이 연결된 전체 흐름이 보이도록 캡처]*

### 실행 결과 화면
> 📸 *[여기에 Make 실행 히스토리(History) 화면 스크린샷 삽입 — high/low 두 케이스가 각각 성공(초록색)으로 표시된 로그, 그리고 Google Sheets에 기록된 결과, Slack 알림 수신 화면 삽입]*
> ⚠️ 스크린샷에 Webhook URL, 이메일 등 민감정보가 노출되지 않도록 마스킹(`***`) 처리할 것

---

## 4. [Zapier] 구현

| 단계 | 앱 | 설정 요약 |
|---|---|---|
| Trigger | Webhooks by Zapier – Catch Hook | Webhook URL 발급 후 테스트 페이로드 전송 |
| 조건 분기 | Paths | Path A(`urgency` = high), Path B(`urgency` = low) 조건 설정 |
| Action 1a | Google Sheets – Create Spreadsheet Row | "긴급문의" 탭에 기록 |
| Action 2a | Slack – Send Channel Message | 🚨긴급 채널에 알림 |
| Action 1b | Google Sheets – Create Spreadsheet Row | "일반문의" 탭에 기록 |
| Action 2b | Slack – Send Channel Message | 일반 채널에 알림 |

### 워크플로우 구성 화면
> 📸 *[여기에 Zap 편집 화면 스크린샷 삽입 — Trigger, Path A/B, 각 Path 내 Action들이 보이도록 캡처]*

### 실행 결과 화면
> 📸 *[여기에 Zapier Task History 화면 스크린샷 삽입 — high/low 두 케이스가 각각 성공(Success)으로 표시된 로그, Google Sheets 기록 결과, Slack 알림 수신 화면 삽입]*
> ⚠️ 계정 이메일, Webhook URL 등은 일부 가림 처리할 것

---

## 5. 비교 분석

| 비교 항목 | Make | Zapier |
|---|---|---|
| **UI/UX** | 시각적 노드(캔버스) 기반, 모듈 간 흐름을 직접 그려서 파악하기 쉬움 | 리스트/스텝 기반, 위에서 아래로 순차적으로 읽는 방식 |
| **설정 난이도** | 초반 학습 곡선이 있으나 Router/Filter 조합이 유연함 | 직관적이고 진입장벽이 낮음, 다만 복잡한 분기는 Paths 기능(유료 플랜 필요할 수 있음)에서 제약 |
| **연동 서비스 범위** | Google, Slack 등 주요 서비스는 물론 세부 API 커스텀 연동(HTTP 모듈)이 강력함 | 앱 마켓플레이스가 매우 넓고 앱 수 자체는 더 많은 편 |
| **무료 플랜 범위** | 월 1,000 Operations 무료 제공 | 월 100 Tasks 무료 제공 (분기 시 Task 소모가 더 빠름) |
| **실행 로그 확인 방식** | History 탭에서 각 모듈별 입출력 데이터(Input/Output)를 상세히 확인 가능 | Task History에서 Task 단위로 성공/실패 여부와 데이터 확인 |
| **에러 핸들링** | 모듈별 Error Handler 라우트 구성 가능, 재시도 옵션 세밀하게 설정 | 자동 재시도(Auto-replay) 기능 제공, 설정은 비교적 단순 |

### 도구별 장단점

**Make**
- 장점: 복잡한 분기·반복 로직을 시각적으로 설계하기 쉬움, 무료 Operations 한도가 넉넉함
- 단점: 초기 UI가 다소 복잡하게 느껴질 수 있음

**Zapier**
- 장점: 직관적인 UI로 빠르게 진입 가능, 문서와 커뮤니티 자료가 풍부함
- 단점: 무료 Task 한도가 적어 분기가 많은 워크플로우에서는 금방 소진됨

### 적합한 상황 의견
- **Make**: 조건 분기가 많고 복잡한 워크플로우를 무료 범위에서 최대한 구현하고 싶은 경우, 또는 API 커스텀 연동이 필요한 경우에 적합하다.
- **Zapier**: 단순한 단일 흐름 자동화를 빠르게 만들고 싶거나, 비개발자가 처음 자동화를 접하는 경우에 적합하다.

---

## 6. 과금 리스크 및 대안
- 두 도구 모두 무료 플랜 범위(Make 1,000 Ops/월, Zapier 100 Tasks/월) 내에서 본 워크플로우 구현이 가능하다.
- 만약 Zapier에서 Paths(분기) 기능이 유료 플랜 전용으로 제한될 경우, 대안으로 Filter를 활용한 2개의 별도 Zap(high용/low용)으로 분리 구현할 수 있다.
