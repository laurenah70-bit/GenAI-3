# 프로젝트 1: 자동화 도구 비교 구현
### 고객 문의 접수 자동 분류 및 알림 워크플로우

- **작성자**: [이름을 입력하세요]
- **제출일**: [YYYY-MM-DD]
- **비교 대상 도구**: Make, Zapier

---

## 1. 개요

- **워크플로우 목적**: 외부에서 접수되는 고객 문의를 긴급도(urgency)에 따라 자동 분류하여 스프레드시트에 기록하고, 담당 채널에 Slack 알림을 발송한다.
- **구현 조합**: Webhook(Trigger) + Router/Filter(조건 분기) + Google Sheets(기록) + Slack(알림)
- **동일성 보장 기준**: 두 도구는 동일한 입력 스키마, 동일한 분기 조건(urgency 값), 동일한 출력 포맷(Sheets 컬럼 구조, Slack 메시지 형식)을 사용해 "같은 워크플로우"임을 보장한다.

### 입력 스키마 (양 도구 공통)
```json
{
  "name": "string",
  "email": "string",
  "category": "string",
  "urgency": "high | low",
  "message": "string"
}
```

### 출력 포맷 (양 도구 공통)
- **Google Sheets 컬럼**: `날짜 | 이름 | 이메일(마스킹) | 분류 | 긴급도 | 문의내용`
- **Slack 메시지 템플릿(긴급)**: `🚨 [긴급] {category} 문의 - {name} / 담당자 @{assignee} 확인 바랍니다.`
- **Slack 메시지 템플릿(일반)**: `[신규] {category} 문의 - {name} 접수되었습니다.`

---

## 2. 워크플로우 설계 및 단계별 입출력 예시

```
[Trigger] Webhook으로 문의 데이터 수신
     │
     ▼
[조건 분기] urgency 필드 확인
     │                    │
   "high"                "low"
     │                    │
     ▼                    ▼
[Action 1a] Sheets      [Action 1b] Sheets
"긴급문의" 탭에 기록      "일반문의" 탭에 기록
     │                    │
     ▼                    ▼
[Action 2a] Slack        [Action 2b] Slack
🚨긴급 채널 알림           일반 채널 알림
```

| 단계 | 입력 예시 | 출력 예시 |
|---|---|---|
| Trigger (Webhook) | `{"name":"홍길동","email":"hong***@example.com","category":"환불문의","urgency":"high","message":"결제 후 상품이 오지 않았습니다."}` | 파싱된 JSON 객체를 다음 모듈로 전달 |
| 조건 분기 | `urgency = "high"` | 경로 A로 라우팅 (경로 B는 스킵) |
| Sheets 기록 | 위 JSON 필드 | 시트에 1개 행 추가, 응답으로 `row_id` 반환 |
| Slack 알림 | `row_id`, `name`, `category` | 채널에 메시지 게시, 메시지 `ts`(타임스탬프) 반환 |

### 복합 조건(다중 필드) 처리 기준
실무에서는 `urgency` 외에 `category`(예: "결제" 관련은 항상 긴급 취급)도 함께 고려할 수 있다. 이 경우 조건 결합 규칙은 다음 우선순위를 따른다.

1. **1순위**: `category = "결제"` → 무조건 긴급 경로 (urgency 값과 무관)
2. **2순위**: `urgency = "high"` → 긴급 경로
3. **기본값**: 위 조건에 해당하지 않으면 일반 경로

Make/Zapier 모두 Filter 조건을 `OR` 로직으로 묶어 `category = 결제 OR urgency = high` 형태로 구현한다.

**테스트용 Webhook 페이로드 (urgency 분기 검증용)**
```json
// 케이스 1 (긴급 경로 트리거)
{"name":"홍길동","email":"hong***@example.com","category":"환불문의","urgency":"high","message":"결제 후 상품이 오지 않았습니다."}

// 케이스 2 (일반 경로 트리거)
{"name":"김철수","email":"kim***@example.com","category":"배송문의","urgency":"low","message":"배송 예정일이 궁금합니다."}
```
두 케이스를 각각 1회 이상 전송하여 두 분기가 모두 실행되었음을 확인한다.

---

## 3. [Make] 구현

| 단계 | 모듈 | 설정 요약 |
|---|---|---|
| Trigger | Webhooks – Custom webhook | Webhook URL 발급 후 테스트 페이로드 전송 |
| 조건 분기 | Router | 2개 경로로 분기, 각 경로에 Filter(`urgency = high`, `urgency = low`) 설정 |
| Action 1a | Google Sheets – Add a Row | "긴급문의" 탭에 name/email/category/message 기록 |
| Action 2a | Slack – Create a Message | 🚨긴급 채널에 알림 |
| Action 1b | Google Sheets – Add a Row | "일반문의" 탭에 기록 |
| Action 2b | Slack – Create a Message | 일반 채널에 알림 |

### 📸 구성 화면 캡처 체크리스트
- [ ] 전체 시나리오 캔버스(Webhook → Router → 4개 Action 모듈이 한 화면에 연결된 상태)
- [ ] Router의 Filter 조건 설정 상세 화면 (urgency = high / low 각각)
- [ ] Google Sheets 모듈의 필드 매핑 설정 화면
- [ ] Slack 모듈의 메시지 템플릿 설정 화면

### 📸 실행 결과 캡처 체크리스트
- [ ] History 탭에서 "케이스 1(high)" 실행 로그 — 각 모듈 Input/Output 데이터 포함
- [ ] History 탭에서 "케이스 2(low)" 실행 로그 — 각 모듈 Input/Output 데이터 포함
- [ ] Google Sheets에 실제로 기록된 두 행(긴급문의 탭 1행, 일반문의 탭 1행)
- [ ] Slack 채널에 게시된 두 알림 메시지(긴급/일반 각각)

> ⚠️ 위 항목이 모두 채워져야 "실행 증거 제출" 요건이 충족됩니다. 마스킹 예시: Webhook URL은 `https://hook.make.com/****...`, 이메일은 `hong***@example.com`처럼 앞 2~3자만 노출합니다.

---

## 4. [Zapier] 구현

| 단계 | 앱 | 설정 요약 |
|---|---|---|
| Trigger | Webhooks by Zapier – Catch Hook | Webhook URL 발급 후 테스트 페이로드 전송 |
| 조건 분기 | Paths | Path A(`urgency` = high), Path B(`urgency` = low) 조건 설정 |
| Action 1a | Google Sheets – Create Spreadsheet Row | "긴급문의" 탭에 기록 |
| Action 2a | Slack – Send Channel Message | 🚨긴급 채널에 알림 |
| Action 1b | Google Sheets – Create Spreadsheet Row | "일반문의" 탭에 기록 |
| Action 2b | Slack – Send Channel Message | 일반 채널에 알림 |

### 📸 구성 화면 캡처 체크리스트
- [ ] 전체 Zap 편집 화면(Trigger → Path A/B → 각 Action이 보이는 상태)
- [ ] Path A/B 각각의 조건 설정 상세 화면
- [ ] Google Sheets 액션의 필드 매핑 화면
- [ ] Slack 액션의 메시지 템플릿 화면

### 📸 실행 결과 캡처 체크리스트
- [ ] Task History에서 "케이스 1(high)" 성공 로그
- [ ] Task History에서 "케이스 2(low)" 성공 로그
- [ ] Google Sheets에 실제로 기록된 두 행
- [ ] Slack 채널에 게시된 두 알림 메시지

> ⚠️ 계정 이메일, Webhook URL 등은 Make 항목과 동일한 방식으로 마스킹 처리합니다.

---

## 5. 비교 분석

| 비교 항목 | Make | Zapier | 비고 |
|---|---|---|---|
| **UI/UX** | 시각적 노드(캔버스) 기반 | 리스트/스텝 기반 | 정성적 평가 |
| **설정 난이도** | 학습곡선 있으나 유연 | 진입장벽 낮음 | 초심자 체감 난이도 |
| **연동 서비스 범위** | 약 2,500개 이상 앱/서비스 (자체 발표 기준, 확인 필요) | 약 7,000개 이상 앱 (자체 발표 기준, 확인 필요) | *실제 수치는 각 사 공식 페이지에서 재확인 권장* |
| **무료 플랜 범위** | 월 1,000 Operations | 월 100 Tasks | Zapier는 분기(Path) 1회 실행 시 Task 소모량이 더 큼 |
| **분기 1회 실행당 소모량** | Operations 약 4~5개 소모(모듈 수 기준) | Task 약 4~5개 소모 | 본 워크플로우 기준 추정치, 실측 필요 |
| **실행 로그 확인 방식** | History에서 모듈별 Input/Output 상세 확인 | Task History에서 Task 단위 확인 | Make가 더 세분화된 디버깅 가능 |
| **에러 핸들링/재시도** | Error Handler 라우트 + 재시도 횟수 설정 가능 | Auto-replay(자동 재시도) 제공, 세부 설정 제한적 | - |
| **협업 기능** | 팀 단위 시나리오 공유, 버전 히스토리 제공 | Zap 공유는 가능하나 버전 관리 기능은 상대적으로 단순 | 협업 시 Make가 유리 |
| **디버깅 편의성** | 모듈별 재실행(Run this module only) 가능 | 전체 Zap 재실행 위주 | 부분 재실행 가능 여부가 디버깅 시간에 영향 |

> 📌 표의 정량 수치(앱 개수, Operations/Task 소모량)는 참고용 추정치입니다. 실제 구현 후 각 도구의 대시보드에서 정확한 소모량을 확인해 표를 업데이트하세요.

### 도구별 장단점

**Make**
- 장점: 복잡한 분기·반복 로직을 시각적으로 설계하기 쉬움, 무료 Operations 한도가 넉넉함, 모듈별 부분 재실행으로 디버깅이 빠름
- 단점: 초기 UI가 다소 복잡하게 느껴질 수 있음

**Zapier**
- 장점: 직관적인 UI로 빠르게 진입 가능, 문서와 커뮤니티 자료가 풍부함
- 단점: 무료 Task 한도가 적어 분기가 많은 워크플로우에서는 금방 소진됨, 부분 재실행이 어려워 디버깅 시간이 상대적으로 길어질 수 있음

### 적합한 상황 의견
- **Make**: 조건 분기가 많고 복잡한 워크플로우를 무료 범위에서 최대한 구현하고 싶은 경우, 팀 단위로 협업하며 버전을 관리해야 하는 경우에 적합하다.
- **Zapier**: 단순한 단일 흐름 자동화를 빠르게 만들고 싶거나, 비개발자가 처음 자동화를 접하는 경우에 적합하다.

---

## 6. 모듈화 및 재사용 전략

같은 분기 로직(urgency 확인)을 여러 워크플로우에서 재사용해야 할 경우를 대비한 모듈화 기준은 다음과 같다.

| 재사용 단위 | 인터페이스(입력/출력 계약) | 재사용 예시 |
|---|---|---|
| **분기 판별 모듈** | 입력: `{urgency, category}` / 출력: `{route: "urgent"|"normal"}` | 문의 접수뿐 아니라 주문 취소, 클레임 처리 워크플로우에도 동일 판별 로직 재사용 |
| **기록 모듈(Sheets)** | 입력: 표준화된 레코드 객체 / 출력: `row_id` | 시트 대상만 바꿔서 다른 업무의 이력 기록에 재사용 |
| **알림 모듈(Slack)** | 입력: `{channel, template_id, variables}` | 템플릿 ID만 교체하면 다른 알림 유형에도 재사용 |

**Zapier에서 별도 Zap(high용/low용)으로 분리하는 방식의 구체적 기준**
- 분리 기준: 처리 로직이 5단계 이상으로 길어지거나, 서로 다른 팀이 각 경로를 관리해야 할 때 분리한다.
- 인터페이스 통일: 두 Zap 모두 동일한 Webhook 스키마를 입력으로 받도록 강제하여, 상위 라우팅 Zap이 `urgency` 값에 따라 적절한 Zap의 Webhook URL로 재전달(relay)하는 구조를 사용한다.
- 재사용 단위: "Sheets 기록 + Slack 알림" 조합을 하나의 서브플로우로 취급하고, 이후 다른 프로젝트에서도 이 2-Action 조합을 복제해 재사용한다.

---

## 7. 과금 리스크 및 대안
- 두 도구 모두 무료 플랜 범위(Make 1,000 Ops/월, Zapier 100 Tasks/월) 내에서 본 워크플로우 구현이 가능하다. 주 1회, 월 4회 실행 기준으로는 두 도구 모두 무료 한도의 5% 미만만 소모할 것으로 추정된다.
- 만약 Zapier에서 Paths(분기) 기능이 유료 플랜 전용으로 제한될 경우, 6절의 모듈화 전략에 따라 별도 Zap 2개(high용/low용)로 분리 구현한다.
