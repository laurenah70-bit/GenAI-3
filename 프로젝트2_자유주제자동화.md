# 프로젝트 2: 자유 주제 자동화 설계 및 구현
### 팀 회의록 자동 정리 및 알림

- **작성자**: [이름을 입력하세요]
- **제출일**: [YYYY-MM-DD]

---

## 1. 반복 업무 정의

**업무명**: 매주 팀 회의 후 회의록 요약 및 공유

**현재 방식(수작업)**
1. 회의가 끝나면 Google Docs에 회의록을 작성한다.
2. 회의록에서 핵심 결정사항과 담당자를 사람이 직접 골라낸다.
3. Slack 팀 채널에 수동으로 요약을 타이핑해 공유한다.
4. 별도 스프레드시트에 회의록 이력을 손으로 기록한다.

**문제점**: 회의가 잦을수록 요약·공유·기록에 매번 5~10분이 소요되며, 담당자가 바쁠 경우 누락되기 쉽다. 주간 회의 기준 월 4회 반복 시 약 20~40분의 업무 시간이 소모된다.

---

## 2. 도구 선정 및 이유

**선정 도구**: Make

**선정 이유 (프로젝트 요구사항과 수치로 매칭)**

| 요구사항 | Make의 대응 | 수치 근거 |
|---|---|---|
| 주 1회, 월 4회 자동 실행 | 무료 플랜 월 1,000 Operations | 본 워크플로우는 실행당 약 4개 모듈(Trigger+파싱+분기+2Action) 사용 → 월 4회 실행 시 약 16 Operations 소모, 무료 한도의 2% 미만 |
| 조건 분기(결정사항 유무) 시각적 설계 | Router 기능 | 코드 없이 캔버스에서 분기 경로를 직접 그려서 확인 가능 |
| Google Docs·Sheets·Slack 연동 | 기본 커넥터 지원 | 별도 API 키 발급 없이 OAuth 연동만으로 3개 서비스 모두 연결 가능 |

---

## 3. 워크플로우 설계

```
[Trigger] Google Docs – Watch Documents
          (지정 폴더에 새 문서 생성 감지)
     │
     ▼
[Action 1] 텍스트 파싱
          (제목 / 참석자 / 결정사항 추출)
     │
     ▼
[조건 분기] 결정사항이 존재하는가?
     │                     │
    Yes                    No
     │                     │
     ▼                     ▼
[Action 2] Slack           [Action 2'] Slack
요약 + 담당자 멘션 발송      "결정사항 없음" 알림만 발송
     │                     │
     ▼                     ▼
[Action 3] Google Sheets – Add a Row
          (회의 날짜 / 제목 / 요약 / 결정사항 여부 기록)
```

### 흐름 설명 및 Action 1(텍스트 파싱) 세부 처리 순서
1. **Trigger**: 지정된 Google Drive 폴더에 새 회의록 문서가 생성되면 자동으로 감지한다.
2. **Action 1 (파싱) — 세부 단계**
   - 1) 문서 전체 텍스트를 가져온다 (Google Docs – Get Document Content).
   - 2) 정규식 `제목:\s*(.+)` 로 제목 필드를 추출한다.
   - 3) 정규식 `참석자:\s*(.+)` 로 참석자 목록을 추출해 배열로 변환한다.
   - 4) 정규식 `결정사항:\s*([\s\S]*?)(?=\n\n|$)` 로 결정사항 블록을 추출한다.
   - 5) 추출된 결정사항 문자열의 공백을 제거(trim)한 후, 길이가 0이면 "없음"으로 표준화한다.
3. **조건 분기**: 결정사항 텍스트가 "없음"이 아닌 경우에만 담당자 멘션이 포함된 상세 알림을 보내고, "없음"인 경우 간단한 알림만 보낸다.
4. **Action 2**: Slack 팀 채널에 요약 메시지를 자동 게시한다.
5. **Action 3**: 모든 회의는 분기 결과와 무관하게 Google Sheets에 이력으로 기록되어 검색·추적이 가능하다.

### 단계별 입출력 예시
```
[Trigger 출력]
{ "doc_id": "1AbCdEf...", "doc_title": "8/18 주간 회의록", "created_at": "2026-08-18T09:00:00+09:00" }

[Action 1 출력]
{
  "title": "8월 3주차 정기회의",
  "attendees": ["홍길동", "김철수"],
  "decision": "다음 스프린트부터 배포 주기를 주 1회로 변경"
}

[Slack 메시지 예시 - 결정사항 있음]
"📋 8월 3주차 정기회의 요약\n결정사항: 다음 스프린트부터 배포 주기를 주 1회로 변경\n담당자: @홍길동 확인 부탁드립니다."

[Slack 메시지 예시 - 결정사항 없음]
"📋 8월 3주차 정기회의가 등록되었습니다. (별도 결정사항 없음)"
```

---

## 4. 구현

| 단계 | 모듈(Make) | 설정 요약 |
|---|---|---|
| Trigger | Google Docs – Watch Documents | 회의록 저장 폴더를 감시 대상으로 지정 |
| Action 1 | Text Parser | 정규식으로 제목/참석자/결정사항 추출 (3.절 세부 단계 참고) |
| Router/Filter | Router | 결정사항 텍스트("없음" 여부)로 2개 경로 분기 |
| Action 2 | Slack – Create a Message | 팀 채널에 요약 + 담당자 @멘션 게시 |
| Action 3 | Google Sheets – Add a Row | 회의 이력 시트에 날짜/제목/요약 기록 |

### 📸 구현 화면 캡처 체크리스트
- [ ] 전체 시나리오 캔버스 (Watch Documents → Text Parser → Router → Slack/Sheets 흐름이 한 화면에 보이는 상태)
- [ ] Text Parser 모듈의 정규식 설정 화면
- [ ] Router의 분기 조건("없음" 여부) 설정 화면
- [ ] Slack 메시지 템플릿 설정 화면(멘션 변수 포함)
- [ ] Google Sheets 필드 매핑 화면

---

## 5. 실행 결과

**테스트 케이스**
1. 결정사항이 포함된 회의록 문서 생성 → 상세 알림 분기 실행 확인
2. 결정사항이 없는 회의록 문서 생성 → 간단 알림 분기 실행 확인

### 📸 실행 결과 캡처 체크리스트 (무인 실행 증거 포함)
- [ ] Make History 탭 — 케이스 1(결정사항 있음) 실행 로그, **자동 트리거 발생 시각**이 문서 생성 시각과 일치함을 보여주는 화면
- [ ] Make History 탭 — 케이스 2(결정사항 없음) 실행 로그, 동일하게 자동 트리거 시각 포함
- [ ] Slack 채널에 게시된 요약 메시지 스크린샷 (담당자 멘션 포함 버전)
- [ ] Slack 채널에 게시된 요약 메시지 스크린샷 (결정사항 없음 버전)
- [ ] Google Sheets에 기록된 회의 이력 2행(케이스 1, 2 모두)

> ⚠️ "자동 트리거 시점"이 사람이 수동으로 실행 버튼을 누른 게 아니라 문서 생성 이벤트로 자동 발동됐음을 보여주는 것이 핵심 증거입니다. Make History에서 실행 시작 시각(Started at)과 Trigger 유형(Instant/Scheduled)이 함께 보이도록 캡처하세요.
> ⚠️ 계정 이메일, 폴더 링크 등 민감정보는 마스킹(`***`) 처리할 것

---

## 6. 보너스 과제 적용 (선택)

### 6-1. AI 연동 Action

**적용 방안**: 결정사항 추출 단계(Action 1)를 정규식 대신 ChatGPT/Claude API 호출로 대체한다.

**정규식 방식의 구체적 한계 사례**
| 사례 | 정규식이 실패하는 이유 |
|---|---|
| "결정사항:" 대신 "결론:" 또는 "액션아이템:"으로 작성된 회의록 | 사전에 정의한 키워드 패턴과 다르면 매칭 실패 |
| 결정사항이 여러 문단에 걸쳐 서술형으로 작성된 경우 | 정규식은 고정된 블록 경계(빈 줄 등)를 기준으로 자르므로 문맥을 이해하지 못해 일부만 추출됨 |
| 담당자가 "@이름" 형식이 아니라 "이름 님이 진행"처럼 자연어로 표현된 경우 | 담당자 추출용 정규식이 매칭하지 못해 멘션 없이 알림 발송됨 |

**AI Action 도입 시 처리 방식**: 문서 전체 텍스트를 프롬프트로 전달해 `{title, attendees, decision, assignee}` 형태의 JSON을 반환하도록 요청하면, 표현 방식이 달라져도 핵심 정보를 안정적으로 추출할 수 있다.

**단계별 실행 일정 및 책임 분배**
| 단계 | 내용 | 예상 소요 | 책임자 |
|---|---|---|---|
| 1 | 회의록 샘플 10건 수집 및 다양한 표현 패턴 확인 | 1일 | 워크플로우 담당자 |
| 2 | AI 프롬프트 설계 및 JSON 출력 포맷 확정 | 1일 | 워크플로우 담당자 |
| 3 | Make에 HTTP/AI 모듈 연결 및 API 키 등록(민감정보이므로 Make의 Connection 기능으로 안전하게 저장) | 0.5일 | 자동화 구현 담당자 |
| 4 | 정규식 방식과 AI 방식 병행 테스트(A/B) 후 정확도 비교 | 1일 | 자동화 구현 담당자 |
| 5 | 정식 전환 및 모니터링 1주 | 지속 | 팀 전체 |

### 6-2. 실패 알림 및 재시도 전략

**적용 방안**: Google Sheets 기록(Action 3)이 실패할 경우 Make의 Error Handler 라우트를 연결하여 관리자에게 실패 알림 메일을 보내고, 임시 시트에 데이터를 대체 저장한다.

**재시도 정책(구체 수치)**
- **재시도 횟수**: 최대 3회
- **대기 전략**: 지수 백오프(Exponential Backoff) — 1차 실패 후 30초 대기, 2차 실패 후 2분 대기, 3차 실패 후 5분 대기
- **최종 실패 시**: 3회 모두 실패하면 즉시 관리자에게 이메일 알림을 발송하고, 원본 데이터를 "임시_회의록_백업" 시트에 저장하여 데이터 유실을 방지한다.
- **알림 내용**: 실패 시각, 실패한 모듈명, 원본 회의록 문서 링크(마스킹 처리)를 포함한다.
