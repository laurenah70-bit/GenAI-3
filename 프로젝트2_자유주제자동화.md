# 프로젝트 2: 자유 주제 자동화 설계 및 구현
### 팀 회의록 자동 정리 및 알림

---

## 1. 반복 업무 정의

**업무명**: 매주 팀 회의 후 회의록 요약 및 공유

**현재 방식(수작업)**
1. 회의가 끝나면 Google Docs에 회의록을 작성한다.
2. 회의록에서 핵심 결정사항과 담당자를 사람이 직접 골라낸다.
3. Slack 팀 채널에 수동으로 요약을 타이핑해 공유한다.
4. 별도 스프레드시트에 회의록 이력을 손으로 기록한다.

**문제점**: 회의가 잦을수록 요약·공유·기록에 매번 5~10분이 소요되며, 담당자가 바쁠 경우 누락되기 쉽다.

---

## 2. 도구 선정 및 이유

**선정 도구**: Make

**선정 이유**
- 조건 분기(결정사항 유무 확인)를 시각적으로 설계하기 쉬워 워크플로우 파악이 직관적이다.
- 무료 Operations(월 1,000회) 범위 내에서 주간 단위 실행이 충분히 가능하다.
- Google Docs, Slack, Google Sheets 모두 기본 커넥터로 지원되어 추가 개발 없이 연동 가능하다.

---

## 3. 워크플로우 설계

```
[Trigger] Google Docs – Watch Documents
          (지정 폴더에 새 문서 생성 감지)
     │
     ▼
[Action 1] 텍스트 파싱
          (제목 / 참석자 / 결정사항 추출)
     │
     ▼
[조건 분기] 결정사항이 존재하는가?
     │                     │
    Yes                    No
     │                     │
     ▼                     ▼
[Action 2] Slack           [Action 2'] Slack
요약 + 담당자 멘션 발송      "결정사항 없음" 알림만 발송
     │                     │
     ▼                     ▼
[Action 3] Google Sheets – Add a Row
          (회의 날짜 / 제목 / 요약 / 결정사항 여부 기록)
```

**흐름 설명**
1. **Trigger**: 지정된 Google Drive 폴더에 새 회의록 문서가 생성되면 자동으로 감지한다.
2. **Action 1 (파싱)**: 문서 내용에서 제목, 참석자, 결정사항 항목을 텍스트 패턴 기반으로 추출한다.
3. **조건 분기**: 결정사항 텍스트가 비어 있지 않은 경우에만 담당자 멘션이 포함된 상세 알림을 보내고, 없는 경우 간단한 알림만 보낸다.
4. **Action 2**: Slack 팀 채널에 요약 메시지를 자동 게시한다.
5. **Action 3**: 모든 회의는 분기 결과와 무관하게 Google Sheets에 이력으로 기록되어 검색·추적이 가능하다.

---

## 4. 구현

| 단계 | 모듈(Make) | 설정 요약 |
|---|---|---|
| Trigger | Google Docs – Watch Documents | 회의록 저장 폴더를 감시 대상으로 지정 |
| Action 1 | Text Parser | 정규식으로 "결정사항:" 이후 텍스트 추출 |
| Router/Filter | Router | 결정사항 텍스트 존재 여부로 2개 경로 분기 |
| Action 2 | Slack – Create a Message | 팀 채널에 요약 + 담당자 @멘션 게시 |
| Action 3 | Google Sheets – Add a Row | 회의 이력 시트에 날짜/제목/요약 기록 |

### 구현 화면
> 📸 *[여기에 Make 시나리오 편집 화면 스크린샷 삽입 — Watch Documents → Text Parser → Router → Slack/Sheets 전체 흐름이 보이도록 캡처]*

---

## 5. 실행 결과

**테스트 케이스**
1. 결정사항이 포함된 회의록 문서 생성 → 상세 알림 분기 실행 확인
2. 결정사항이 없는 회의록 문서 생성 → 간단 알림 분기 실행 확인

> 📸 *[여기에 Make 실행 히스토리 화면 스크린샷 삽입 — 두 테스트 케이스가 각각 성공으로 표시된 로그]*

> 📸 *[여기에 Slack 채널에 게시된 요약 메시지 스크린샷 삽입 (담당자 멘션 포함 버전 / 미포함 버전 각각)]*

> 📸 *[여기에 Google Sheets에 기록된 회의 이력 스크린샷 삽입]*

> ⚠️ 스크린샷에는 실제 계정 이메일, Webhook URL 등 민감정보가 노출되지 않도록 마스킹(`***`) 처리할 것

---

## 6. 보너스 과제 적용 (선택)

- **AI 연동 Action**: 결정사항 추출 단계(Action 1)를 정규식 대신 ChatGPT/Claude API 호출로 대체하여, 비정형 회의록 텍스트에서도 결정사항과 담당자를 자동 요약하도록 확장 가능하다.
- **실패 알림 및 재시도**: Google Sheets 기록(Action 3)이 실패할 경우 Make의 Error Handler 라우트를 연결하여 관리자에게 실패 알림 메일을 보내고, 임시 시트에 데이터를 대체 저장하도록 설계할 수 있다.
